import React from 'react'
import './styles.css'

export const Title2 = ({ text, styleClass }) => (
  <h2 className={styleClass}>{text}</h2>
)
export const Title3 = ({ text, styleClass }) => (
  <h3 className={styleClass}>{text}</h3>
)
export const Title4 = ({ text, styleClass }) => (
  <h4 className={styleClass}>{text}</h4>
)
export const Title5 = ({ text, styleClass }) => (
  <h5 className={styleClass}>{text}</h5>
)
